let a = function
  | _ -> ()
  | _ -> ()

let a = function
  | _ -> ()
  | _ -> ()

let a =
  a |> fun x ->
    let a = b in
    y

let a =
  a |> fun x ->
    let a = b in
    y

let a =
  map li (fun x ->
    let a = b in
    y )

let a =
  map li
    (fun x ->
    let a = b in
    y )

let a =
  map liiiiiiiiiiiiiiiiiiii li
    liiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiii
    (fun x ->
    let a = b in
    y )

let a =
  map liiiiiiiiiiiiiiiiiiii li
    liiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiii
    (fun
      xxxxxxxxxxxxxxxxxxxxxxxxx
      xxxxxxxxxxxxxxxxxxxxxxxxx
      xxxxxxxxxxxxxxxxxxxxxx
      xxxxxxxxxxxxxxxxxxxxx
    ->
    let a = b in
    y )

let a =
  map
    (fun
      xxxxxxxxxxxxxxxxxxxxxxxxx
      xxxxxxxxxxxxxxxxxxxxxxxxx
      xxxxxxxxxxxxxxxxxxxxxx
      xxxxxxxxxxxxxxxxxxxxx
    ->
    let a = b in
    y )
